<?xml version="1.0" encoding="utf-8"?>
<!--{FlexFormatter: IgnoreFile}-->
<mx:Application creationComplete="init('there is a value to send to the init')"
					 layout="absolute"
					 xmlns:mx="http://www.adobe.com/2006/mxml    f">
	<mx:Script>
		<![CDATA[
			//comment location
	if (x) y++;
	while (true)
	z--;
	for (var i:int=2;i<10;i++)
	trace(i);
	do x++; while (i<10);
	if (x) trace(z);
		else if (y) switch (x){ case 1: z--; break;}
		else m--;
	if (m) trace(x); else {trace(y);}
	do {x++;} while (i<10);
	while (true)
	 while (false)
	 	for (;i<10;i--)
	 		if (x)
	 		trace(z)
	 		else trace(m);
		]]>
	</mx:Script>

	<mx:Script>
		<![CDATA[
			//comment location
	if (x) y++;
	while (true)
	z--;
	for (var i:int=2;i<10;i++)
	trace(i);
	do x++; while (i<10);
	if (x) trace(z);
		else if (y) switch (x){ case 1: z--; break;}
		else m--;
	if (m) trace(x); else {trace(y);}
	do {x++;} while (i<10);
	while (true)
	 while (false)
	 	for (;i<10;i--)
	 		if (x)
	 		trace(z)
	 		else trace(m);
		]]>
	</mx:Script>
	<mx:Button other="some other value"
				  x="10"
				  y="90"
				  id=""/>
</mx:Application>
